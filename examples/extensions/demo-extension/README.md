# Demo Extension

This folder is a ready-to-install OpenCowork extension.

## Install

1. Open OpenCowork.
2. Go to Settings -> Extensions.
3. Click Install folder.
4. Select this `examples/extensions/demo-extension` folder.
5. Enable Demo Extension.

## Tools

- `extension__demo-extension__get_post`

The HTTP tool uses `https://jsonplaceholder.typicode.com`, which is declared in `permissions.network`.
